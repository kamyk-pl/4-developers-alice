extends layout.pug

block vars
  - var title = 'onSelect'

block title
        | onSelect
block content
         section
            pre
             code.smaller-code(data-trim data-noescape data-line-numbers="12-14" )
               |
               | function init() {
               |  ...
               |
               |  const light = new THREE.HemisphereLight(0xffffff, 0xbbbbff, 1);
               |  light.position.set(0.5, 1, 0.25);
               |  scene.add(light);
               |
               |  controller = renderer.xr.getController(0);
               |  controller.addEventListener('select', onSelect);
               |  scene.add(controller);
               |
               |  function onSelect() {
               |        drawScene();
               |   }
               |
               | ...
               |
