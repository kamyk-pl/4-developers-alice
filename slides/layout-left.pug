-var transition= "slide"
block vars


- var pageId = title+ "-"+  counter++
section(id= pageId, data-transition=transition).center
  h2.header
    block title
  div(style="text-align: left;")
    block content
